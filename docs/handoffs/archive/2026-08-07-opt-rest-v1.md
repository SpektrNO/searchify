# Handoff archive: opt-rest-v1-search + opt-rest-v1-index

Completed 2026-08-07.

## Changes

- `POST /v1/search` — JSON twin of `search_local` (Bearer auth)
- `POST /v1/index` — JSON twin of `index_paths` (Bearer auth)
- Shared `executeSearchLocal` used by MCP tool and REST
- JSON `{ "error": "..." }` for 400/401/405/500 on REST (and 401 for all Bearer routes)
- README curl examples; architecture updated
- MCP server version 0.6.0

## Verification

- `go test ./...` (including REST auth/validation/happy-path tests)
- `go build -o bin/searchify ./cmd/searchify`

## Follow-ups

- `POST /v1/remove`, `GET /v1/status`
